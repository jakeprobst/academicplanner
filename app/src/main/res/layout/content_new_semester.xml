<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:showIn="@layout/activity_new_semester"
    tools:context="org.thisnamesucks.academicplanner.NewSemesterActivity">

    <AutoCompleteTextView
        android:id="@+id/autocomplete_school"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:hint="Enter School Name"
        android:textSize="20dp" />

    <Spinner
        android:id="@+id/semester_spinner"
        android:layout_below="@+id/autocomplete_school"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/drop_down_background" />

    <EditText
        android:id="@+id/startdate_btn"
        android:layout_below="@id/semester_spinner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clickable="true"
        android:textSize="18sp"
        android:focusable="false"
        android:editable="false"
        android:inputType="none"
        />

    <EditText
        android:id="@+id/enddate_btn"
        android:layout_below="@id/startdate_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:focusable="false"
        android:editable="false"
        android:inputType="none"
        android:textSize="18sp"
        />
</RelativeLayout>